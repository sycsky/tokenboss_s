/**
 * Login screen — rewritten in Phase 8 (Task 8.1) for code-based auth.
 * Stubbed to keep the build clean while api.ts / auth.tsx are updated.
 */
export default function Login() {
  return <div>Login (rewrite in Phase 8)</div>;
}
