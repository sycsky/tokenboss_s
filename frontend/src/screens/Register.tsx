/**
 * Register screen — rewritten in Phase 8 (Task 8.2) for code-based auth.
 * Stubbed to keep the build clean while api.ts / auth.tsx are updated.
 */
export default function Register() {
  return <div>Register (rewrite in Phase 8)</div>;
}
