node_modules/
dist/
.aws-sam/
samconfig.toml
*.log
.env
.env.local
.env.production
*.tsbuildinfo
.DS_Store
.playwright-mcp/
.worktrees/
.gstack/
vendor/

# Claude Code session state, custom slash commands, vendored skills
.claude/

# OpenSpec spec-driven workflow working dir
openspec/

# brainstorming visual companion
.superpowers/

# Playwright screenshots
*.png
!frontend/src/assets/**/*.png
!frontend/public/**/*.png

# SQLite dev DB + WAL/SHM (schema is recreated at startup via store.ts init()).
# Match both `backend/data/*.db` (when started from backend dir) and root-level
# `data/*.db` (when started from repo root with relative SQLITE_PATH).
backend/data/*.db
data/*.db
*.db-shm
*.db-wal

# Sibling projects (separate repos)
clawd-on-desk/
new-api/

# Vite config compiled outputs (vite.config.ts is the source of truth)
frontend/vite.config.js
frontend/vite.config.d.ts
