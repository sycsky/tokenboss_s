{
  "id": "tokenboss-router",
  "name": "TokenBoss Router",
  "description": "TokenBoss edge router — local LLM proxy for TokenBoss relay (fork of ClawRouter).",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "routing": {
        "type": "object",
        "description": "Override default routing configuration"
      }
    }
  },
  "uiHints": {}
}
